<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.productionchain.ProductlistActivity" >

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/productslist_cancel" >
    </ListView>

    <Button
        android:id="@+id/productslist_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@android:id/list"
        android:layout_alignParentTop="true"
        android:text="Cancel" />

    <Button
        android:id="@+id/productslist_save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/productslist_add"
        android:layout_alignBottom="@+id/productslist_add"
        android:layout_alignRight="@android:id/list"
        android:text="Save" />

    <Button
        android:id="@+id/productslist_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/productslist_cancel"
        android:layout_alignBottom="@+id/productslist_cancel"
        android:layout_centerHorizontal="true"
        android:text="Add" />

</RelativeLayout>